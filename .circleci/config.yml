# This config is equivalent to both the '.circleci/extended/orb-free.yml' and the base '.circleci/config.yml'
version: 2.1

jobs:
  build-for-linux:
    docker:
      - image: cimg/base:2021.07
    steps:
      - run: apt-get install build-essential
      - run: apt-get install autoconf automake gdb git libffi-dev zlib1g-dev libssl-dev
      - run: mkdir build
      - run: cd build
      - run: cmake ..
      - run: make

workflows:
  version: 2.1
  build:
    jobs:
      - build-for-linux